const adminBtn = document.getElementById(adminPage); //Cibler le Bouton

adminBtn.addEventListener('click', function(event)); {//Au click sur le bouton    
// afficher un champ d'input

if{ //Si le mdp est le bon      => afficher la page admin.php

}
else{ //Sinon afficher mesage d'erreur
     
}

}